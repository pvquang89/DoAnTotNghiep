@model DoAnTotNghiep.Models.DTO.LoginModels

@{
    ViewData["Title"] = "Đăng nhập";
}
<!-- Section: Design Block -->
<section class="">
    <!-- Jumbotron -->
    <div class="px-4 py-5 px-md-5 text-center text-lg-start" style="background-color: hsl(0, 0%, 96%)">
        <div class="container">
            <div class="row gx-lg-5 align-items-center">
                <div class="col-lg-6 mb-5 mb-lg-0">
                    <h1 class="my-5 display-3 fw-bold ls-tight">
                        Trao cơ hội <br />
                        <span class="text-primary">tiếp nối ước mơ</span>
                    </h1>
                    <p style="color: hsl(217, 10%, 50.8%)">
                        Tìm việc làm nhanh 24h, việc làm mới nhất trên toàn quốc.
                        Nền tảng kết nối doanh nghiệp và các ứng viên tiềm năng.
                    </p>
                </div>

                <div class="col-lg-6 mb-5 mb-lg-0">
                    <div class="card">
                        <div class="card-body py-5 px-md-5">
                            <form asp-action="Login" asp-controller="Auth" method="post">
                                <!-- Email input -->
                                <div class="form-outline mb-4">
                                    <input asp-for="Email" value="@ViewBag.Email" placeholder="Địa chỉ Email" class="form-control" required />
                                </div>

                                <!-- Password input -->
                                <div class="form-outline mb-4">
                                    <input asp-for="Password" value="@ViewBag.Password" placeholder="Mật Khẩu" type="password" class="form-control" required />
                                </div>

                                <div class="form-group">
                                    <div class="custom-control custom-checkbox small">
                                        @Html.CheckBox("rememberMe", false, new { @class = "custom-control-input", id = "rememberMe" })
                                        <label class="custom-control-label" for="rememberMe">Nhớ tài khoản</label>
                                    </div>
                                </div>

                                <!-- Submit button -->
                                <button type="submit" class="btn btn-primary btn-block mb-4">
                                    Đăng nhập
                                </button>
                                 <p style="color: red">@ViewBag.thongbao</p>
                            </form>
                              <div class="text-center">
                                <p class="mb-2">Bạn chưa có tài khoản? <a asp-controller="Auth" asp-action="Register" style="color: red;">Đăng ký ngay</a></p>
                                <p class="mb-0"><a asp-controller="Auth" asp-action="ForgotPass" style="color: red;">Quên mật khẩu?</a></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Jumbotron -->
</section>
<!-- Section: Design Block -->