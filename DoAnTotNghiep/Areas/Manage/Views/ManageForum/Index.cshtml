@model IPagedList<DoAnTotNghiep.Models.EntityModels.Discuss>
@using X.PagedList
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Manage/Views/Shared/_AdminLayout.cshtml";
     @addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
    @addTagHelper *, AuthoringTagHelpers
}
<h2>Quản lý diễn đàn</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Tiêu đề</th>
            <th>Ngày tạo</th>
            <th>Trạng thái</th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var disscus in Model)
        {
            <tr>
                <td>@disscus.Title</td>
                <td>@disscus.CreatedAt</td>
                @if (disscus.Status == true)
                {
                    <td>Đã Duyệt</td>
                     <td>
                    <a href="@Url.Action("Delete", "ManageForum", new { @id = disscus.DiscussID })" class="btn btn-primary">Xem</a>
                    <a href="@Url.Action("ToggleStatus", "ManageForum", new { @id = disscus.DiscussID })" class="btn btn-danger">
                        Hủy duyệt
                    </a>
                </td>
                }
                else
                {
                    <td>Chưa Duyệt</td>
                     <td>
                    <a href="@Url.Action("Delete", "ManageForum", new { @id = disscus.DiscussID })" class="btn btn-danger">Xem</a>
                    <a href="@Url.Action("ToggleStatus", "ManageForum", new { @id = disscus.DiscussID })" class="btn btn-info">
                        Duyệt
                    </a>
                </td>
                }
            </tr>
        }
    </tbody>
</table>
<div class="pagination">
    <nav aria-label="Page navigation">
        <ul class="pagination">
            <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                <a class="page-link" href="@Url.Action("Index", new { page = Model.PageNumber - 1 })" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                </a>
            </li>
            @for (int i = 1; i <= Model.PageCount; i++)
            {
                <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                    <a class="page-link" href="@Url.Action("Index", new { page = i })">@i</a>
                </li>
            }
            <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                <a class="page-link" href="@Url.Action("Index", new { page = Model.PageNumber + 1 })" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                </a>
            </li>
        </ul>
    </nav>
</div>
